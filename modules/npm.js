// npm -> node package manager 
// helps us install, manage and share packages for node js, it also manages the dependencies.
// package: folder containing libraries
// dependencies: 

// advantages of npm:
// can reuse the code, can share code, 

console.log("Hello");
// npm init: initializes the file as node js project (R) so that we can import packages, etc
// npm init -y: 

// Modules: reuseable block of code

// 1. Core/Built-in module: no installation req (ex fs-create file system, os , http, url, path-handle all the files)
// 2. Local/own/custom modules: 
// 3. Third party modules: (ex express)

// // coreModule fs
// var fs = require('fs');
// fs.writeFileSync("Example.txt", "conetent 02");
// // import export from src to dest

// // coreModule os
// const os = require('os');
// console.log(os.platform());
// console.log(os.hostname());
// console.log(os.cpus());

// local modules..
// math.js

// const math = require('./math.js');
// console.log(math.add(10,20));
// console.log(math.substract(10,20));
// console.log(math.divide(10,20));
// console.log(math.multiply(10,20));

// third party module : created by other developers , availble to us in npm registry?? , need installation 
// ex exprees, mongoose,  pdf creater node?
// cat.js











