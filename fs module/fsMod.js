const fs = require('fs');
fs.writeFileSync("empty.txt", "The fs module in Node.js is a built-in core module that provides a comprehensive API for interacting with the file system, enabling operations like reading, writing, creating, deleting, and managing files and directories. It supports both synchronous and asynchronous methods, allowing developers to choose based on performance and application needs.");


console.log(fs);